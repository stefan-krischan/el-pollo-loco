{
  "compilerOptions": {
    "target": "ES6", // Das ECMAScript-Ziel, z. B. ES5, ES6, ESNext
    "module": "commonjs", // Modul-Format: commonjs, ESNext, usw.
    "checkJs": false, // Aktiviert Fehlerüberprüfung in JavaScript-Dateien
    "baseUrl": "./", // Basis-URL für Modulauflösung
    "paths": {
      "@/*": ["scripts//*"] // Alias für den Pfad
    },
    "strict": true, // Aktiviert strenge Typenüberprüfung
    "esModuleInterop": true // Erlaubt die Interoperabilität mit CommonJS-Modulen
  },
  "include": [
    "./**/*" // Alle Dateien und Ordner im `src`-Verzeichnis
  ],
  "exclude": [
    "node_modules", // Ausschließen des `node_modules`-Ordners
    "dist" // Ausschließen des Build-Ordners
  ]
}
